pub type UserId = u16; // Represents a user ID in the network as a 16-bit unsigned integer for speed of lookup.
pub type ChannelId = u16; // Represents a channel ID in the network as a 16-bit unsigned integer for speed of lookup.

pub mod error;
pub mod logging;
